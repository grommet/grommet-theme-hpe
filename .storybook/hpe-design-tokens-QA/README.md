This is a temporary solution for QAing `hpe-design-tokens` package prior to release.
This should be removed once the `.github/workflows/update-design-tokens-stable.yaml`
workflow has been re-instated in the `grommet/hpe-design-system` repository.

Until then, we must build the `hpe-design-tokens`, copy and paste the `dist` into this directory for QA purposes.
